import {VoltageAndCurrents} from "./VoltageAndCurrents";

export type Connect = {
    count44PinCPU: number,
    count62PinPCIE: number,
    count16PinPCIE50: number,
    molexCount: number,
    sataCount: number,
    count24pin: number,
}